import smtplib, ssl
from email.message import EmailMessage

def saada_email(saaja_email):
    teema = "Test kiri Pythonist"
    saatja_email = "polina.gussarova@gmail.com"
    parool = input("Sisesta rakenduse parool: ")

    smtp_server = "smtp.gmail.com"
    port = 587
    context = ssl.create_default_context()

    msg = EmailMessage()
    msg["Subject"] = teema
    msg["From"] = saatja_email
    msg["To"] = saaja_email

    with open(r"C:\Users\opilane\source\repos\penguiiin.jpg", "rb") as f:
        image_data=f.read()
    msg.add_attachment(image_data, maintype="image", subtype="jpg")

    try:
        with smtplib.SMTP(smtp_server, port) as server:
            server.starttls(context=context)
            server.login(saatja_email, parool)
            server.send_message(msg)
        print("E-kiri saadetud!")
    except Exception as e:
        print(f"Midagi läks valesti... {e}")

kellele = input("Sisesta saaja e-posti aadress: ")
saada_email(kellele)


### Проблемы с кодировкой

Если русский текст отображается неправильно:

```
File →SaveAs →Encoding →UTF-8

```

### Отправка проекта в GitHub

- Add to Source Control
- Description — краткое описание проекта
- Repository → Public
- Create and Push

📌 **Commit** — это сохранение изменений

📌 **Push** — отправка на GitHub

---

## 2. Переменные и выражения

### Переменная

Переменная — это **ячейка памяти**, в которой хранится значение.

```python
x =10
name ="Alex"

```

Python сам определяет тип данных.

### Выражение

Выражение — это вычисление, которое даёт результат.

```python
a =5 +3 *2

```

Сначала умножение, потом сложение.

---

## 3. Арифметические операции

| Операция | Пример | Результат |
| --- | --- | --- |
| + | 5 + 2 | 7 |
| - | 5 - 2 | 3 |
| * | 5 * 2 | 10 |
| / | 5 / 2 | 2.5 |
| // | 5 // 2 | 2 |
| % | 5 % 2 | 1 |
| ** | 2 ** 3 | 8 |

---

## 4. Типы данных

### 🔢 int — целые числа

Используются для:

- счётчиков,
- индексов,
- количества.

```python
a =10
b =int("5")

```

---

### 🌊 float — дробные числа

Используются для:

- измерений,
- денег,
- точных расчётов.

```python
x =3.14
y =round(x,2)

```

⚠️ Возможны погрешности:

```python
0.1 +0.2 ≠0.3

```

---

### 🧵 str — строки (текст)

```python
text ="Hello"

```

Операции:

```python
"Hi" +"!"# Hi!
"ha" *3# hahaha

```

Полезные методы:

```python
text.upper()
text.lower()
text.replace("a","o")
text.split()

```

Форматирование:

```python
name ="Alex"
print(f"Привет, {name}")

```

---

### ⚙️ bool — логический тип

```python
True
False

```

Используется в условиях.

Ложные значения:

- 0
- ""
- []
- None

---

## 5. Ввод и вывод данных

### input()

Всегда возвращает **строку**.

```python
age =int(input("Введите возраст: "))

```

### print()

Выводит информацию на экран.

```python
print("Возраст:", age)

```

---

## 6. Условия (if)

Условие — это проверка.

```python
if условие:
    действие
elif другое:
    действие
else:
    действие

```

Пример:

```python
age =17

if age >=18:
print("Совершеннолетний")
else:
print("Несовершеннолетний")

```

---

## 7. Операторы сравнения и логики

### Сравнение

- `==` равно
- `!=` не равно
- `<` меньше
- `>` больше
- `<=`
- `>=`

### Логические операторы

- `and` — И
- `or` — ИЛИ
- `not` — НЕ

```python
if age >18and age <65:

```

---

## 8. Циклы

### while

Повторяет код, пока условие истинно.

```python
i =0
while i <5:
print(i)
    i +=1

```

### while True

Бесконечный цикл.

```python
whileTrue:
    cmd =input()
if cmd =="exit":
break

```

---

### for

Используется, когда известно количество повторов.

```python
for iinrange(5):
print(i)

```

`range(start, stop, step)`

---

## 9. Списки (list)

Список — это набор значений.

```python
nums = [1,2,3]

```

Методы:

```python
nums.append(4)
nums.remove(2)
nums.sort()

```

Индексы начинаются с **0**.

```python
nums[0]
nums[-1]

```

---

## 10. Функции

Функция — это блок кода, который можно вызывать.

```python
defsum(a, b):
return a + b

```

Без `return` → `None`.

---

## 11. Лямбда-функции

Короткие функции в одну строку.

```python
add =lambda a, b: a + b

```

---

## 12. Модули

Модуль — это файл с кодом.

```python
import math

```

### math

- `sqrt()`
- `pow()`
- `pi`

### random

- `randint()`
- `random()`

---

## 13. Программа из двух файлов

### module.py

```python
defhello():
print("Hello")

```

### main.py

```python
from moduleimport hello
hello()

```

---

## 14. Отправка e-mail через Python

Используются модули:

- `smtplib`
- `email.message`

Алгоритм:

1. Создать письмо
2. Указать отправителя и получателя
3. Подключиться к SMTP
4. Отправить письмо

Используется **пароль приложения**, а не обычный.

### Теория

Переменная — это имя, под которым хранится значение в памяти.

```python
x =10
name ="Alex"

```

Выражение — это вычисление, которое даёт результат.

```python
a =5 +3 *2

```

---

# **ЗАДАНИЯ С РЕШЕНИЯМИ**

## 1.Переменные и выражения

### 🧪 Задание Арифметика

**Условие:**

Создай переменные `a = 8`, `b = 3`.

Выведи сумму, разность, произведение и деление.

✅ **Решение**

```python
a =8
b =3

print(a + b)
print(a - b)
print(a * b)
print(a / b)

```

---

## 2. Типы данных

### int — целые числа

Используются для счёта и индексов.

```python
x =5
y =int("10")

```

### float — дробные числа

Используются для измерений и денег.

```python
pi =3.14
print(round(pi,2))

```

### str — строки

Текст в кавычках.

```python
text ="Python"

```

### bool — логический тип

Значения: `True`, `False`.

---

### 🧪 Задание Ввод чисел

**Условие:**

Попроси пользователя ввести два числа и выведи их сумму.

✅ **Решение**

```python
a =int(input("Введите первое число: "))
b =int(input("Введите второе число: "))

print("Сумма:", a + b)

```

---

## 3. Ввод и вывод данных

### input()

Всегда возвращает строку.

### print()

Выводит данные на экран.

---

### 🧪 Задание Ввод текста

**Условие:**

Запроси имя и возраст, выведи приветствие.

✅ **Решение**

```python
name =input("Введите имя: ")
age =input("Введите возраст: ")

print(f"Привет, {name}! Тебе{age} лет.")

```

---

## 4. Условия (if)

### Теория

Условия используются для принятия решений.

```python
if условие:
    действие
else:
    действие

```

Операторы сравнения:

`== != < > <= >=`

---

### 🧪 Задание Проверка возраста

**Условие:**

Если возраст ≥ 18 — доступ разрешён.

✅ **Решение**

```python
age =int(input("Возраст: "))

if age >=18:
print("Доступ разрешён")
else:
print("Доступ запрещён")

```

---

### 🧪 Задание Чётное или нечётное

**Условие:**

Определи, чётное ли число.

✅ **Решение**

```python
n =int(input("Введите число: "))

if n %2 ==0:
print("Чётное")
else:
print("Нечётное")

```

---

## 5. Логические операторы

- `and` — И
- `or` — ИЛИ
- `not` — НЕ

---

### 🧪 Задание Рабочий возраст

**Условие:**

Возраст от 18 до 65 — рабочий.

✅ **Решение**

```python
age =int(input("Возраст: "))

if age >=18and age <=65:
print("Рабочий возраст")

```

---

## 6. Циклы

### while

Повторяет код, пока условие истинно.

### for

Используется, когда известно количество повторений.

---

### 🧪 Задание while

**Условие:**

Вывести числа от 1 до 10.

✅ **Решение**

```python
i =1
while i <=10:
print(i)
    i +=1

```

---

### 🧪 Задание Сумма чисел

**Условие:**

Найти сумму чисел от 1 до N.

✅ **Решение**

```python
n =int(input("Введите N: "))
s =0

for iinrange(1, n +1):
    s += i

print("Сумма:", s)

```

---

### 🧪 Задание Команды

**Условие:**

Программа работает, пока не введут `exit`.

✅ **Решение**

```python
whileTrue:
    cmd =input("Введите команду: ")
if cmd =="exit":
break

```

---

## 7. Списки (list)

### Теория

Список — это упорядоченный набор значений.

```python
nums = [1,2,3]

```

Индексы начинаются с 0.

---

### 🧪 Задание Работа со списком

**Условие:**

Вывести первый, последний элемент и сумму списка.

✅ **Решение**

```python
nums = [2,4,6,8,10]

print(nums[0])
print(nums[-1])
print(sum(nums))

```

---

### 🧪 Задание Максимум

**Условие:**

Найти максимальное число в списке.

✅ **Решение**

```python
nums = [5,12,3,9]
print(max(nums))

```

---

## 8. Функции

### Теория

Функция — это блок кода, который можно вызывать.

```python
defsumma(a, b):
return a + b

```

---

### 🧪 Задание Функция суммы

✅ **Решение**

```python
defsumma(a, b):
return a + b

print(summa(3,4))

```

---

### 🧪 Задание Проверка чётности

✅ **Решение**

```python
defis_even(n):
return n %2 ==0

```

---

## 9. Мини-проекты

### 🧪 Задание Калькулятор

✅ **Решение**

```python
a =float(input("a: "))
b =float(input("b: "))
op =input("Операция: ")

if op =="+":
print(a + b)
elif op =="-":
print(a - b)
elif op =="*":
print(a * b)
elif op =="/":
print(a / b)
else:
print("Ошибка")

```

---

### 🧪 Задание Угадай число

✅ **Решение**

```python
import random

num = random.randint(1,10)

whileTrue:
    guess =int(input("Угадай число: "))
if guess == num:
print("Угадал!")
break
else:
print("Попробуй ещё")
```
